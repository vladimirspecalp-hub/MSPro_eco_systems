Здравствуйте, Ирина!

Спасибо за инструкцию. Я попробовал настроить всё сам (включая PassengerFriendlyErrorPages), но ошибка 504 сохраняется. Видимо, есть нюансы окружения.

Вы написали, что можете развернуть проект из исходника.
Я загрузил полный архив проекта в корень моего аккаунта:
`/home/m/mspro/deploy_archive.tar.gz`

В архиве:
- Исходный код
- Скомпилированная папка `dist` (точка входа `dist/index.js`)
- `package.json`

**Требования:**
- Node.js версия: 20 или выше (лучше 22/24, но 20 LTS тоже подойдет).
- Запуск через: `npm run start` (или `node dist/index.js`).
- Порт: приложение слушает `process.env.PORT` или 5000.

Буду благодарен, если вы развернете его на домене mspro-ltd.ru.
