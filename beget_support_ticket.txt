Тема: Не запускается Node.js сайт (ошибка 504) — конфликт версий Node/Passenger

Здравствуйте!

Пытаюсь развернуть Node.js приложение на домене mspro-ltd.ru.
Столкнулся с тем, что сайт не грузится (504 Gateway Time-out или просто "Connection Timed Out"), хотя все файлы загружены и зависимости установлены.

**Параметры сайта:**
1. **Домен:** mspro-ltd.ru
2. **Технологии:** Node.js (Express + Vite build), MySQL (Drizzle ORM).
3. **Точка входа:** `dist/index.js` (скомпилированный JS файл).

**Что мы уже сделали:**
1. Загрузили файлы сборки в `~/mspro-ltd.ru/public_html/`.
2. Установили Node.js v24.13.0 через NVM (так как системная версия Node.js, видимо, старая).
3. Успешно выполнили `npm install` и миграции базы данных (подключение к БД `mspro_ltd` проходит успешно через скрипт `install_node.sh`).
4. Создали `.htaccess` для запуска через Phusion Passenger:
   ```apache
   PassengerEnabled on
   PassengerAppRoot /home/m/mspro/mspro-ltd.ru/public_html
   PassengerAppType node
   PassengerStartupFile dist/index.js
   # Пытались указать путь к NVM ноде, но не уверены, что это работает корректно:
   PassengerNodejs /home/m/mspro/.nvm/versions/node/v24.13.0/bin/node
   ```

**В чем проблема:**
При попытке открыть сайт он "висит" и падает по таймауту.
Подозреваю, что Passenger либо не видит правильную версию Node.js (нужна поддержка ES Modules и современного синтаксиса), либо приложение падает при старте, но я не могу найти логи ошибок запуска (в стандартном error.log пусто или общие ошибки).

**Вопросы:**
1. Как правильно настроить Passenger, чтобы он использовал установленную через NVM версию Node.js (v24+)?
2. Где посмотреть stderr/stdout лог запуска именно самого Node.js приложения, чтобы увидеть причину падения?
3. Нужно ли открывать какой-то порт или Passenger сам проксирует запросы в приложение? (В коде мы слушаем `process.env.PORT` или 5000).

Спасибо!
